header.header    
    div(ng-controller="MainController")
        nav(ng-model = "way")
            -var url = "http://{{way.name}}/{{way.lang}}/"
            ul.nav
                each item in ["pizza", "drinks", "salads"]
                    li
                        a(href = url + item) #{item}